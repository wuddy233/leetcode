class Solution {
    public int maxSubArray(int[] nums) {
        //dp[i]表示截止到i，最大的子数组之和
        int[] dp = new int[nums.length];
        int res = nums[0];
        dp[0] = nums[0];
        for(int i = 1;i < nums.length; i++) {
            dp[i] = Math.max(nums[i], dp[i -1] + nums[i]);
            res = Math.max(res, dp[i]);
        }
        return res;
    }
}
